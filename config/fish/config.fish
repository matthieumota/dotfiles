set PATH $HOME/.composer/vendor/bin $PATH
set PATH /usr/local/opt/ruby/bin $PATH
set PATH /usr/local/lib/ruby/gems/(ruby -e 'print Gem.ruby_api_version')/bin $PATH
# Ruby is managed by user on Arch Linux, composer as well
# It can be too on macOS but on native Ruby
# set PATH (ruby -e 'print Gem.user_dir')/bin $PATH
